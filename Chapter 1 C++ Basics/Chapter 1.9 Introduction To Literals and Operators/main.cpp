/*
* Introduction to literals and ooperators in C++

* literal (literal constant)
    - fixed value inserted directly into the src code
    - can be printed out directly
    - constant

* variable:
    - must be fetched from memory and can be fetched on demand
    - can be changed

* expression
    - a non empty sequence of literals, variables, operators, and function calls 
        that calculate a value
    - evaluation = process of executing an expression
    - Results Of an Expression:
        - value
        - object of a function
        - nothing
    - Expression Statement
        - a statement that consists of an expression followed by a semicolon
    

What is the difference between a statement and an expression?
- Statements are used when we want the program to perform an action
- Expressions are used to compute a value

*/